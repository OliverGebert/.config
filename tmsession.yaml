session_name: 3-pane-split
windows:
  - window_name: dev .config
    window_index: 1
    layout: main-horizontal
    options:
      main-pane-height: 65%
    shell_command_before:
      - cd ~/.config/ # run as a first command in all panes
    panes:
      #- focus: true
      - nv . # pane no. 1
      - lg   # pane no. 2
      - la   # pane no. 3
